{
  "name": "workspace-agent",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "broker": "pnpm --filter @workspace-agent/broker start",
    "daemon": "pnpm --filter @workspace-agent/daemon cli",
    "app": "pnpm --filter workspace-agent-app",
    "typecheck": "pnpm -r typecheck"
  }
}
